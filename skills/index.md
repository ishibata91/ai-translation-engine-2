# Skills

このディレクトリは repo ローカル skill の一覧です。`skills/<name>/SKILL.md` と `references/*.md` は `docs-site` の Starlight ビルドで自動的にページ化されます。

## 読み方

- 各 skill の入口は `skills/<name>/SKILL.md`
- 補助テンプレートや説明は同じ skill ディレクトリ配下の Markdown として扱う
- 新しい skill を追加したら `SKILL.md` を置くだけで sidebar に出る
