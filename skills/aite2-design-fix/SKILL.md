---
name: aite2-design-fix
description: AI Translation Engine 2 専用。既存 UI の見た目やレイアウト、余白、配置、視認性などのデザイン修正を行うための簡易 skill。対象ファイルをユーザーに指定してもらい、Playwright MCP で確認しながら直すときに使う。
---

# AITE2 Design Fix

この skill はデザイン修正用。
今は簡易草案として、何を扱うかだけを定義する。

## 使う場面
- 既存 UI のレイアウトや見た目を直したい
- 表示崩れ、余白、配置、視認性を改善したい
- デザイン差分をピンポイントで修正したい

## 必須ルール
- 対象ファイルは必ずユーザーに指定してもらう
- 対象画面は `wails dev` で起動した状態を前提にする
- 修正前後の確認に Playwright MCP を必ず使う

## やること
- 対象ファイルを確認する
- `wails dev` で起動した対象画面を前提に確認する
- 画面上の問題を特定する
- 最小差分で見た目を修正する
- Playwright MCP で修正結果を確認する

## 起動コンテキスト
- ユーザーが `wails dev` でアプリを起動している前提で進める
- Playwright MCP は起動済みの画面を観測し、修正前後を確認するために使う
- デザイン修正のために起動手段を勝手に変えない

## 原則
- 作業は対話内でタスク化し、常に 1 ステップずつ進める
- 一度に複数箇所へ広げず、指定された対象から順に直す
- 各ステップで対象、観測結果、次の 1 手を明確にする
- 指定されていないファイルへ勝手に範囲を広げない
- デザイン修正にロジック変更を混ぜない
- Playwright MCP で実画面確認するまで完了扱いにしない
