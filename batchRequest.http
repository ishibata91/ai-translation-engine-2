### 変数定義（環境変数として設定可能）
@batchName =batches/rb21sy6ag247bvu3k1nmoc6itlvsmke1c5qc
@apiKey =AIzaSyDm--UFcRlW1Xwd_Uwg88kISLJZBZJqM1E
@baseUrl = https://generativelanguage.googleapis.com/v1beta
@waitMs = {{30*1000}}

### ステータス確認リクエスト（複数実行でポーリング）
GET {{baseUrl}}/{{batchName}}
x-goog-api-key: {{apiKey}}

> {%
    client.global.set("response", response.body);
    client.log("Current state: " + JSON.parse(response.body).state);
%}

### 完了チェック（スクリプトで条件分岐、Run Allでループ相当）
###
# 上記リクエストを複数回実行（例: 10回分コピーしてポーリング）
# またはRun All Requests使用。[web:33][web:6]
