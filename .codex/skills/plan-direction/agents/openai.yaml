display_name: plan-direction
short_description: ユーザー入口として plan lane だけを受け、下流作業を直接せずに routing と conflict 停止を担う
default_prompt: Use $plan-direction at skills/plan-direction as an orchestration-only user entry for design, spec completion, and docs sync. If the request is actually implementation or bugfix work, stop immediately and return a handoff to the correct direction skill instead of doing downstream work directly.
