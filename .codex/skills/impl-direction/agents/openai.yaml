display_name: impl-direction
short_description: ユーザー入口として impl lane だけを受け、下流実装を直接せずに routing と conflict 停止を担う
default_prompt: Use $impl-direction at skills/impl-direction as an orchestration-only user entry for implementation, UI reflection, and frontend or backend task starts. If the request is actually planning or bugfix work, stop immediately and return a handoff to the correct direction skill instead of doing downstream work directly.
