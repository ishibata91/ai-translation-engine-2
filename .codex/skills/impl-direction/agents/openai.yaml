display_name: impl-direction
short_description: 実装入口の routing と plan への差し戻し判断を担う
default_prompt: Use $impl-direction at skills/impl-direction to check artifact readiness, route implementation through $impl-distill and $impl-workplan, and hand off to $plan-distill or $plan-sync when implementation cannot proceed directly.
