display_name: impl-review
short_description: 実装差分を required delta 中心でレビューし、結果だけを返す
default_prompt: Use $impl-review at skills/impl-review to compare masked implementation artifacts against the spec and return structured review feedback plus docs sync need without applying fixes or editing the diff.
