display_name: impl-frontend-work
short_description: frontend の所有範囲だけを実装する
default_prompt: Use $impl-frontend-work at skills/impl-frontend-work to implement only the owned frontend paths from a work order and return a structured diff plus verification.
