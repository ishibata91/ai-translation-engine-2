display_name: fix-work
short_description: bugfix の確定済み修正方針だけを実装する
default_prompt: Use $fix-work at skills/fix-work when `sub_implementer` should apply only the confirmed bugfix plan, stay within the owned files, and return verification results.
