display_name: impl-distill
short_description: 確定済み artifacts を実装 packet へ蒸留する
default_prompt: Use $impl-distill at skills/impl-distill to turn reviewed planning artifacts into an implementation packet for $impl-workplan without making any new design decisions.
