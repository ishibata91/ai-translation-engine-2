## 1. 標準コーディング規約の完成

- [x] 1.1 共通規約ドキュメントを作成し、MUST/SHOULD区分で命名・error wrap・context伝播・構造化ログ・SRP・公開API doc必須を明文化する
- [x] 1.2 リポジトリ固有規約（Interface-First AIDD、Vertical Slice、DTO分離、Pipeline Mapper責務）を同ドキュメントへ統合する
- [x] 1.3 レビュー向けチェックスタイル（確認観点、違反時の扱い、例外運用）を規約に追記し、運用ルールを確定する

## 2. 品質ライブラリの導入

- [x] 2.1 `golangci-lint` の設定ファイルを追加し、`staticcheck` / `govet` / `errcheck` / `revive` / `gosec` を標準lintとして定義する
- [x] 2.2 `goimports` を実行導線に組み込み、フォーマット手順をローカル・CIで統一する
- [x] 2.3 `go.uber.org/goleak` を並行処理関連テストへ導入し、goroutineリーク検知を有効化する
- [x] 2.4 `govulncheck` は任意実行（依存更新時/リリース前）として手順のみ定義し、必須ゲートには含めない

## 3. 実行導線とPR運用の統一

- [x] 3.1 ローカル向けの品質チェック実行コマンド（lint/format/test）をプロジェクト標準として整備する
- [x] 3.2 CIでローカル同等の品質ゲートを実行し、失敗時のブロック条件を定義する
- [x] 3.3 PRテンプレートに規約準拠チェック項目を追加し、レビュー時の判定基準を固定する

## 4. 既存コードへの段階適用

- [ ] 4.1 既存コードの主要違反を棚卸しし、優先度順に是正計画を作成する
- [ ] 4.2 高優先違反（コンパイル安全性・error処理・context/log逸脱）を先行修正する
- [ ] 4.3 lint fail条件を段階的に厳格化し、最終的に必須ゲートを安定運用へ移行する
