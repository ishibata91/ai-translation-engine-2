## 1. バックエンド（Wails/Go）APIの改修

- [x] 1.1 `pkg/dictionary/contract.go` および DTO の定義を修正し、`DictTermPage`（TotalCount付き）の追加とページネーション用パラメータの定義を行う
- [x] 1.2 `pkg/dictionary/store.go` の SQLite クエリを改修し、`GetEntriesBySourceIDPaginated` および全体件数カウント用のメソッドを実装する
- [x] 1.3 `pkg/dictionary/store.go` において、全く新しい全ソース横断検索用のメソッド `SearchAllEntriesPaginated` および全体件数カウントを実装する
- [x] 1.4 `pkg/dictionary/service.go` で上記メソッドの呼び出しをラッピングし、エラーハンドリングなどを実装する
- [x] 1.5 `app.go` の Wails バインディングを更新し、フロント側から `DictGetEntriesPaginated` と `DictSearchAllEntriesPaginated` を呼べるようにする

## 2. フロントエンド: GridEditor UIコンポーネントの改修

- [x] 2.1 `mocks-react/src/components/GridEditor.tsx` から「＋ 行追加」ボタンおよび関連ロジックを完全に削除する
- [x] 2.2 各カラム下部のフィルタ入力欄の挙動を変更し、文字入力時のリアルタイム反映を廃止する
- [x] 2.3 フィルタの「検索」ボタンを配置し、押下時に初めて親コンポーネントまたは内部状態にフィルタ条件を適用するように変更する
- [x] 2.4 フィルタの「クリア」ボタンを追加し、押下時にフィルタ条件を削除して元の表示状態に復帰するように実装する
- [x] 2.5 ページネーション（500件表示単位）に対応できるよう、親から渡されるページ数、全体件数 (`TotalCount`)、およびページ切り替えコールバックを受け取るUIをフッター等に実装する
- [x] 2.6 削除アクション時、即座にリストから消す（および永続化する）のではなく、状態を「未保存（編集中）」のマーク（`isDirty=true`）に切り替え、UI上で削除予定として扱うように変更する
- [x] 2.7 `GridEditor` 内に親から受け取る「戻る」ボタンフックと連携し、編集中 (`isDirty === true`) なら確認の警告モーダルを出すよう実装する
- [x] 2.8 編集中に「もとに戻す（全て元に戻す）」をクリックした際、変更前のデータ（削除取り消しを含む）へ状態を完全にリセットするロジックを組む

## 3. フロントエンド: 辞書ビルダー画面の改修

- [x] 3.1 `frontend/src/pages/DictionaryBuilder.tsx` のインポートファイル選択UI（Dropzone領域など）に対し、Tailwindのクラスを用いて未処理時はコンパクトに（`h-32`等）、必要な時のみ高さが拡張されるように指定を修正する
- [x] 3.2 単一の辞書ソース選択時のエントリ一覧表示処理を、全件取得からバインディングされた `DictGetEntriesPaginated` (ページネーション版) 呼び出しへ変更する
- [x] 3.3 単一辞書ソース表示において、ページ切り替え操作時に再度バックエンドAPIを叩き、テーブルの中身と総件数を更新するロジックを組み込む
- [x] 3.4 辞書ビルダー画面上部に「横断検索」ボタン（トリガー）を新規に配置する

## 4. フロントエンド: 横断検索とモーダルの実装

- [x] 4.1 横断検索の検索条件（キーワード入力）および対象ソースの選択を行う「検索モーダル」コンポーネントを作成する
- [x] 4.2 モーダルからの「検索実行」を受けて、`DictSearchAllEntriesPaginated` APIを呼び出し、結果を保存する状態管理を実装する
- [x] 4.3 検索結果を一時的に表示するため、横断検索用の状態ビュー（`GridEditor` を流用したラップコンポーネント）を作成する
- [x] 4.4 横断検索結果の表示においても、APIを利用した500件単位のページネーションが行えるよう接続する
- [x] 4.5 横断検索ビューからの「編集」「削除」操作（GridEditor内）の保存アクションを、既存の `DictUpdateEntry` や `DictDeleteEntry` と正しく紐づける
- [x] 4.6 横断検索の一時ビューにおいて、「戻る」アクション時の未保存警告と、破棄時の親画面（元の辞書一覧等）への復帰遷移フローを実装する
