## ADDED Requirements

該当なし。

## MODIFIED Requirements

該当なし。

## Notes

- 本 change は `openspec/specs/backend_coding_standards.md` の REQUIREMENTS 自体を変更しない
- 目的は既存 REQUIREMENTS に対する実装準拠の回復であり、新規 capability 追加や既存 capability の振る舞い変更は含まない
- 実装対象と優先度は `violation-targets.md` を正とし、`tasks.md` では package ごとの修正・検証作業へ分解する
